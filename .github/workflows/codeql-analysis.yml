name: CodeQL analysis

on:
  push:
    branches: [test]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 3 * * 0'

jobs:
  call-codeQL-analysis:
    name: CodeQL analysis
    uses: priya-kinthali/reusable-workflows/.github/workflows/codeql-analysis.yml@test-ubuntu24
